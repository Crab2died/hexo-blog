<% if (post.tags && post.tags.length) { %>
    <% post.tags.forEach(function(tag, i) { %>
        <%- link_to(tag.path, tag.name, {class:"post-tag"}) %><% if (i != post.tags.length - 1) { %> | <% } %>
    <% }); %>
<% } %>